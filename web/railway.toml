# Railway configuration for the Next.js frontend

[build]
builder = "NIXPACKS"
buildCommand = "pnpm install && pnpm build"

[build.nixpacks]
NODE_VERSION = "20"

[deploy]
startCommand = "pnpm start -p ${PORT:-3000}"
